fn main() {
    // statement(구문) -> 그 자체.
    let x = 5; // 변수 바인딩

    // expression + ';'
    x;
    x + 1;

    // expression(표현식) -> 반환값 있음
    {}
    let z = {
        5 // expression
    };
    let z = {
        5;
    };
}
